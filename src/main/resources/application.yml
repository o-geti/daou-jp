spring:
  application:
    name: daujapan

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

springdoc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html
